# Use Tomcat 10 for Spring Boot 3.x (Jakarta). For Boot 2.x, switch to tomcat:9.0-jdk11-temurin
FROM tomcat:9.0-jdk17-temurin

# Clean default ROOT app
RUN rm -rf /usr/local/tomcat/webapps/ROOT

# Copy WAR built by Maven into Tomcat; rename to ROOT.war to serve at '/'
COPY target/*.war /usr/local/tomcat/webapps/ROOT.war

EXPOSE 8080
